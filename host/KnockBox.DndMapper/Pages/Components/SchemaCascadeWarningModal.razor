<ConfirmModal IsOpen="IsOpen"
              Title="Change schema?"
              Message="Changing the attribute schema mid-session will rebuild every existing character sheet. Attributes whose name + type match the new schema keep their value. Attributes that don't match are reset to defaults. Attributes not in the new schema are removed. This cannot be undone."
              ConfirmText="Change Schema"
              CancelText="Cancel"
              OnConfirm="OnConfirm"
              OnCancel="OnCancel" />
