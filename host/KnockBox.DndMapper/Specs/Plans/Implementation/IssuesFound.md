No way to drag around the map.
Needs image layer display so that the host can select images that way.
Locked images should not be selectable on the map to promote dragging to move the map entirely.
Add ability for the host to create host-only character sheets to use for NPCs and monsters.
When images are uploaded, they should default to being full-size and preserve their aspect ratio.
When scaling images, the transformation should preserve the aspect ratio. It should only allow the aspect ratio to be changed if the user is holding shift while dragging the transform.
Image options needs a reset-aspect-ratio button to adjust the width or height (whichever causes the area of the image to shrink) to return back to the original aspect ratio of the image.
Grid snapping option for image positions and scaling.
When holding space, mouse movements should pan the map.
When selecting a number in the image properties it should select the whole number initially.