@using KnockBox.Core.Services.Navigation
@using KnockBox.Core.Services.State.Games.Shared

<div class="dtd-header-container">
    <div class="dtd-header-paper-edge"></div>

    <a class="dtd-header-brand" href="@NavigationService.GetHomeUri()" title="Click to return home">
        <span class="dtd-header-brand-icon">👗</span>
        <span class="dtd-header-brand-text">DRAWN TO DRESS</span>
    </a>

    <div class="dtd-header-actions">
        <RoomCodeButton ClassName="dtd-header-code"
                        CoverClassName="dtd-header-code-cover"
                        ValueClassName="dtd-header-code-value"
                        CoverText="Sketch ID" />
        <button class="dtd-header-leave" @onclick="Leave" title="Close Sketchbook" aria-label="Leave Session">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
        </button>
    </div>
</div>

@code {
    [Inject] INavigationService NavigationService { get; set; } = default!;
    [Inject] IGameSessionService GameSessionService { get; set; } = default!;

    private void Leave() => GameSessionService.LeaveCurrentSession(navigateHome: true);
}
