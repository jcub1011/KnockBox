@page "/tools/svg-canvas"

<PageTitle>SVG Canvas Demo</PageTitle>

<div class="svg-canvas-demo">
    <h1>SVG Canvas Demo</h1>
    <p>
        This page hosts the reusable <code>SvgDrawingCanvas</code> component from <code>KnockBox.Core</code>.
        It supports pointer and touch drawing, undo, brush customization, resize handles, and SVG export.
    </p>

    <SvgDrawingCanvas Width="min(100%, 56rem)" Height="28rem" />
</div>
